{
  "installCommand": "cd .. && pnpm install --filter @appresuelve/client --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --filter @appresuelve/client build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
